# Centro-001 Handoff

## Status

GitHub issue `Centro-001` is **complete and verified**.

## Changes applied

- Added pnpm project metadata and scripts in `package.json`.
- Locked the approved minimal dependencies in `pnpm-lock.yaml`:
  Next.js 14, React 18, TypeScript, Tailwind/PostCSS, ESLint, Prisma, and Prisma
  Client.
- Added base configuration:
  `.gitignore`, `.eslintrc.json`, `tsconfig.json`, `next-env.d.ts`,
  `next.config.mjs`, `postcss.config.mjs`, and `tailwind.config.ts`.
- Added `pnpm-workspace.yaml` (auto-generated by `pnpm approve-builds`) and
  `.npmrc` so that `pnpm install` works with the existing lockfile without
  requiring a manual environment override.
- Added the safe environment template `.env.example`, documenting all variables
  from `SPEC.md` section 8 without secrets.
- Updated `docker-compose.yml` to avoid a common local port conflict:
  - Removed the obsolete `version` attribute.
  - Mapped the PostgreSQL container to host port `5433` instead of `5432`.
- Updated `.env.example` and `README.md` to match the new `5433` port.
- Added `prisma/schema.prisma` with only the PostgreSQL datasource and Prisma
  Client generator. Per the agreed scope, there are no models or migrations yet.
- Added the minimal App Router UI in `src/app/` and a server-only Prisma singleton
  in `src/lib/db.ts`.
- Added `README.md` with local setup and verification instructions.

## Verification performed

- `pnpm install` passes.
- `pnpm prisma generate --allow-no-models` passes.
- `pnpm lint` passes.
- `pnpm tsc --noEmit` passes.
- `pnpm build` passes.
- `pnpm test` passes (placeholder).
- `pnpm dev` starts the application and `http://localhost:3000` returns `200 OK`.
- Prisma Client connects to the PostgreSQL container via the configured
  `DATABASE_URL` (`postgresql://centro:centro@localhost:5433/centro?schema=public`).
- `git diff --check` passes.

## Notes

- A local `postgres.exe` process was already listening on port `5432`, so the
  Docker Compose service was mapped to `5433` to prevent a port collision.
- `.env` is generated from `.env.example` and is ignored by Git; it contains the
  local database URL used for verification.
- No application domain code, migrations, Auth.js, shadcn, Zod, TanStack Query,
  or Zustand work was applied; those remain outside Centro-001's infrastructure-only
  scope.

## Next steps

- Review the modified/untracked files.
- Commit the changes (excluding `.env` and build artifacts) when ready.
- Move on to the next open issues: `Centro-002`, `Centro-003`, or `Centro-004`.
